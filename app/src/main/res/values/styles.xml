<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Full-screen theme — hides ActionBar and status bar chrome so the
         web app fills edge-to-edge like a native app -->
    <style name="Theme.RoutePlanner" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar transparent so HTML header bleeds to top edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Prevent white flash on launch -->
        <item name="android:windowBackground">@color/launch_bg</item>
    </style>
</resources>
